#include <stdio.h>

    {
        prinf("entre com o peso em (kg):");
         scanf("%f",&alt);
        printf("entre com a altura em (m):");
        scanf("%f", &alt);

        imc=peso
    }
    \\exemplode caracter
    {
        
    }